#include "vector.hpp"
#include <stdio.h>

int main ( )
  {
    /*
    int size = 4;
    printf ( "%d\n", size );
    double * a = new double[size];
    for ( int i = 0; i < size; i ++ ) a[i] = i+1;
    Vector_t vec1 ( size, a );
    for ( int i = 0; i < size; i ++ ) a[i]*=10;
    Vector_t vec2 ( size, a );
    vec1 = vec1 + vec2;
    vec1.dump ( );
    printf ( "ok...\n" );
    */
    return 0; 
  }
